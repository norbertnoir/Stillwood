using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CutsceneManager : MonoBehaviour
{
    /*
    [SerializeField] Transform camera;

    public bool isCutsceneActive = false;

    public bool isOpenToCutscene = false;

    [SerializeField] float distanceToStartCutscene = 10f;

    void Update()
    {
        return; // Temporarily disable cutscene logic for testing
        if (isOpenToCutscene)
        {
            if ((camera.position - Leszy.Instance.playerCameraTarget.position).magnitude < distanceToStartCutscene)
            {
                isCutsceneActive = true;
                Leszy.Instance.SetStop();

                Player.instance.playerMovement.isFrozen = true; // Zatrzymaj ruch gracza

                // Rozpocznij cutscenke
            }
        }


        if (isCutsceneActive)
        {
            camera.LookAt(Leszy.Instance.playerCameraTarget.position);
        }
    }

    public void EndCutscene()
    {
        isCutsceneActive = false;
        Player.instance.playerMovement.isFrozen = false; // Wznów ruch gracza
    }
*/
}
